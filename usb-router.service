[Unit]
Description=Auto USB Modem Router Setup
After=network.target

[Service]
Type=oneshot
ExecStart=/usr/local/bin/usb-autowarp.sh
RemainAfterExit=yes
# Если модем не нашелся сразу, пробуем снова через 10 секунд
Restart=on-failure
RestartSec=10

[Install]
WantedBy=multi-user.target
